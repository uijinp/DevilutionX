server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # pthread(SharedArrayBuffer) 를 쓰는 wasm 은 교차 출처 격리가 필수다.
    add_header Cross-Origin-Opener-Policy  same-origin  always;
    add_header Cross-Origin-Embedder-Policy require-corp always;

    gzip on;
    gzip_types application/wasm application/javascript application/octet-stream text/html;
    gzip_min_length 1024;

    location = /healthz { return 200 "ok\n"; add_header Content-Type text/plain; }

    # 코드·데이터는 커서 캐시하되, 재배포 시 index.html 이 새 파일을 가리키게 한다.
    location ~* \.(wasm|data|mpq)$ { expires 7d; add_header Cache-Control "public"; 
        add_header Cross-Origin-Opener-Policy  same-origin  always;
        add_header Cross-Origin-Embedder-Policy require-corp always; }
    location = /index.html { expires -1; 
        add_header Cross-Origin-Opener-Policy  same-origin  always;
        add_header Cross-Origin-Embedder-Policy require-corp always; }
}
